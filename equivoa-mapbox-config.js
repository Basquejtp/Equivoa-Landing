// ============================================================
// EQUIVOA - MAPBOX CONFIG
// Fill this in after creating a free account at mapbox.com.
// Find your token: Account -> Tokens -> use the "Default public
// token" (starts with "pk."). Free tier covers roughly 50,000
// map loads a month, plenty for testing and early launch.
//
// Until this is filled in, the Map view on the Find page shows
// a friendly "not switched on yet" message instead of a broken
// map, and List view keeps working exactly as before.
// ============================================================
window.EQUIVOA_MAPBOX_TOKEN = 'YOUR_MAPBOX_TOKEN';
